#!/usr/bin/env perl
use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";

use ExternalStorage::App;

my $app = ExternalStorage::App->new;
my $listen = $ENV{ESS_LISTEN} || 'http://*:8080';

$app->config(hypnotoad => { listen => [$listen] });
$app->start('daemon', '-l', $listen);
